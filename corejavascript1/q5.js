import { a, b } from "./q4"

username = "admin"
password = "12345"

checklogin = username == "admin" ? password == 12345 ? "LOGIN SUCCESSFUL" : "Invalid Credentials": "Invalid Credentials"
console.log(checklogin)
for (a = 1, b = 1; a > 6; a++) {
    console.log(a, b)
}
